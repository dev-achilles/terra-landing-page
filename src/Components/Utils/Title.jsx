import React from "react";

const Title = ({ title }) => {
  return (
    <h3 className="text-lg mt-8 font-alfphabet tracking-wider text-[#FB71AE] uppercase mb-2">
      {title}
    </h3>
  );
};

export default Title;
